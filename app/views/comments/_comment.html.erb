# this is a 'partial' that goes into Post#show
# it will be inside a @post.comments.each do |comment| block
# which seems to load all comments and do for each loop
  <p><strong>Commenter: </strong><%= comment.commenter %></p>
  <p><strong>Comment: </strong><%= comment.body %></p>
  <p><%= link_to "Delete Comment", [comment.post, comment],
              :confirm=>"Are you sure you want to delete this comment?",
              :method => :delete %></p>
